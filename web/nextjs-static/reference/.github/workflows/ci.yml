# GitHub Actions CI — uncomment after configuring your repository secrets.
# Required secrets: none for this workflow.
# Optional: add VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID for deployment.
#
# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#
# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true
#
# jobs:
#   ci:
#     name: Lint, Typecheck & Build
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9
#
#       - uses: actions/setup-node@v4
#         with:
#           node-version-file: .nvmrc
#           cache: pnpm
#
#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile
#
#       - name: Lint
#         run: pnpm lint
#
#       - name: Typecheck
#         run: pnpm typecheck
#
#       - name: Build
#         run: pnpm build
